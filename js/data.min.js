function exportObj(b, c) {
  var d =
      "text/json;charset=utf-8," +
      encodeURIComponent(JSON.stringify(b, null, 2)),
    a = document.createElement("a");
  a.href = "data:" + d;
  a.download = c + ".json";
  a.innerHTML = "download JSON";
  a.click();
  a.remove();
}
function importObj(b, c, d) {
  b = b.files[0];
  var a = new FileReader();
  d &&
    (alert("File name: " + b.name), alert("Last modified: " + b.lastModified));
  a.readAsText(b);
  a.onload = function () {
    c(a.result);
  };
  a.onerror = function () {
    alert("Error: " + a.error);
  };
}
